name: AI Tweet Bot (Stable)

on:
  schedule:
    - cron: '0 0 * * *'   # 09:00 JST
    - cron: '0 1 * * *'   # 10:00 JST
    - cron: '0 3 * * *'   # 12:00 JST
    - cron: '0 5 * * *'   # 14:00 JST
    - cron: '0 7 * * *'   # 16:00 JST
    - cron: '0 9 * * *'   # 18:00 JST
    - cron: '0 11 * * *'  # 20:00 JST
    - cron: '0 13 * * *'  # 22:00 JST
    - cron: '0 15 * * *'  # 00:00 JST
    - cron: '0 17 * * *'  # 02:00 JST
  workflow_dispatch:

jobs:
  ai-tweet-bot:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4
      
    - name: Setup Python Environment
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        
    - name: Install Dependencies
      run: |
        echo "📦 基本パッケージインストール"
        python -m pip install --upgrade pip
        pip install tweepy>=4.14.0 feedparser>=6.0.10 requests>=2.31.0
        echo "✅ パッケージインストール完了"
        
    - name: Run Stable AI Tweet Bot
      env:
        TWITTER_CLIENT_ID: ${{ secrets.TWITTER_CLIENT_ID }}
        TWITTER_CLIENT_SECRET: ${{ secrets.TWITTER_CLIENT_SECRET }}
        TWITTER_BEARER_TOKEN: ${{ secrets.TWITTER_BEARER_TOKEN }}
        TWITTER_ACCESS_TOKEN: ${{ secrets.TWITTER_ACCESS_TOKEN }}
        TWITTER_ACCESS_TOKEN_SECRET: ${{ secrets.TWITTER_ACCESS_TOKEN_SECRET }}
      run: |
        echo "🚀 安定版AI自動ツイートボット開始"
        python src/tweet_bot.py
        echo "✅ 安定版AI自動ツイートボット完了"
