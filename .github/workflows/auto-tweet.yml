name: AI Tweet Bot

on:
  schedule:
    # JST時間での10回投稿スケジュール
    - cron: '0 0 * * *'   # 09:00 JST (00:00 UTC)
    - cron: '0 1 * * *'   # 10:00 JST (01:00 UTC)
    - cron: '0 3 * * *'   # 12:00 JST (03:00 UTC)
    - cron: '0 5 * * *'   # 14:00 JST (05:00 UTC)
    - cron: '0 7 * * *'   # 16:00 JST (07:00 UTC)
    - cron: '0 9 * * *'   # 18:00 JST (09:00 UTC)
    - cron: '0 11 * * *'  # 20:00 JST (11:00 UTC)
    - cron: '0 13 * * *'  # 22:00 JST (13:00 UTC)
    - cron: '0 15 * * *'  # 00:00 JST (15:00 UTC)
    - cron: '0 17 * * *'  # 02:00 JST (17:00 UTC)
  workflow_dispatch:      # 手動実行用

jobs:
  ai-tweet-bot:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4
      
    - name: Setup Python Environment
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        cache: 'pip'
        
    - name: Install Python Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install tweepy>=4.14.0 feedparser>=6.0.10 requests>=2.31.0 python-dotenv>=1.0.0
        
    - name: Run AI Tweet Bot
      env:
        TWITTER_CLIENT_ID: ${{ secrets.TWITTER_CLIENT_ID }}
        TWITTER_CLIENT_SECRET: ${{ secrets.TWITTER_CLIENT_SECRET }}
        TWITTER_BEARER_TOKEN: ${{ secrets.TWITTER_BEARER_TOKEN }}
        TWITTER_ACCESS_TOKEN: ${{ secrets.TWITTER_ACCESS_TOKEN }}
        TWITTER_ACCESS_TOKEN_SECRET: ${{ secrets.TWITTER_ACCESS_TOKEN_SECRET }}
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
      run: |
        echo "🚀 AI自動ツイートボット開始"
        python tweet_bot.py
        echo "✅ AI自動ツイートボット完了"
