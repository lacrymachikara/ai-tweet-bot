name: Auto Tweet Bot (MAINTENANCE MODE)
# 緊急メンテナンス: 無制限投稿を防止

on:
  # 全自動実行を一時停止
  # schedule:
  #   - cron: '0 */2 * * *'  # 無効化
  workflow_dispatch:  # 手動実行のみ残す

jobs:
  maintenance:
    runs-on: ubuntu-latest
    steps:
    - name: System Maintenance Notice
      run: |
        echo "🛠️ システムメンテナンス中"
        echo "無料枠最適化システムへ移行中です"
        echo "新システム: 1日3投稿、高品質重視"
        echo "完了予定: 24時間以内"
